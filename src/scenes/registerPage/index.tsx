import React, { useState } from 'react';
import { Box, Typography, TextField, Button, useMediaQuery, useTheme } from '@mui/material';
import RegisterForm from '@/scenes/registerPage/RegisterForm';



const RegisterPage = () => {
  

  return (
    <Box>
      <RegisterForm/>      
    </Box>
  );
};

export default RegisterPage;
